version: "3"

services:
  frontend:
    build: ./client
    ports:
      - "5173:5173"

  backend:
    build: ./server
    ports:
      - "5000:5000"

  mongo:
    image: mongo
    ports:
      - "27017:27017"